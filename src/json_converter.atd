type create_database = {
  id: string;
}

type databases = {
  id: string;
  _rid: string;
  _self: string;
  _etag: string;
  _colls: string;
  _users: string;
  _ts: int;
}

type list_databases = {
  _rid: string;
  databases <json name="Databases">: databases list;
  _count: int;
}

type create_collection = {
  id: string;
  indexingPolicy: string option;
  partitionKey: string option;
}